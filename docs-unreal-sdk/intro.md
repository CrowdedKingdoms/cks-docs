---
slug: intro
sidebar_position: 1
title: Crowdy Unreal SDK
---

# Crowdy Unreal SDK

Documentation for first-party UE plugin work will live here once the crowdy-unreal
repository joins the mono-repo (parity with CrowdyJS’s split between **management** and
**game** endpoints, plus native UDP).

Front-end / engine developers: fill this tree with:

- Install + module setup in Unreal projects
- Auth + token flows against `cks-management-api`
- Replication + Buddy UDP usage against `cks-game-api` / Buddy servers
