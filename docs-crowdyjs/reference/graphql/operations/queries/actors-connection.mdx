---
id: actors-connection
title: actorsConnection
---


export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>






Relay-style cursor-paginated version of `actors`: lists actors owned by the authenticated user, optionally narrowed by `filter` (appId, avatarId, uuid, chunk). Page forward with `first` (default 50, max 200) and `after` (an opaque cursor from a previous page’s `pageInfo.endCursor`); `totalCount` is the full number of matching actors. Requires a valid game token; only the caller’s own actors are returned (full state included).


```graphql
actorsConnection(
  first: Int
  after: String
  filter: ActorFilterInput
): ActorsConnection!
```


### Arguments

#### [<code style={{ fontWeight: 'normal' }}>actorsConnection.<b>first</b></code>](#first)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int</b></code>](/reference/graphql/types/scalars/int.mdx) <Badge class="badge badge--secondary " text="scalar"/> \{#first\}
Return the first N edges after `after`. Default 50; clamped to 1..200.


#### [<code style={{ fontWeight: 'normal' }}>actorsConnection.<b>after</b></code>](#after)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>String</b></code>](/reference/graphql/types/scalars/string.mdx) <Badge class="badge badge--secondary " text="scalar"/> \{#after\}
Opaque cursor: return edges after this one. Pass a previous page's `pageInfo.endCursor`. Omit for the first page.


#### [<code style={{ fontWeight: 'normal' }}>actorsConnection.<b>filter</b></code>](#filter)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>ActorFilterInput</b></code>](/reference/graphql/types/inputs/actor-filter-input.mdx) <Badge class="badge badge--secondary " text="input"/> \{#filter\}
Optional filters; all provided fields are AND-combined. Omit to page over all of the caller’s actors.




### Type

#### [<code style={{ fontWeight: 'normal' }}><b>ActorsConnection</b></code>](/reference/graphql/types/objects/actors-connection.mdx) <Badge class="badge badge--secondary " text="object"/>
Relay-style cursor-paginated connection over the caller’s actors (Actor). Page with `first`/`after`; cursors are opaque.